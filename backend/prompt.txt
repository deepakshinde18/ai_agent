I want to build an agent with below requirement.
1. User will put in his requirement in natural language. 
2. First i need to do the sanitization, PII detection, injection attacks validation.
3. There will be intent detection which will be used with RAG. We will have to stored the vector in chroma and find the intention using user input.
4. using that intent value i need to fire 1 db query which will give me, target_type, where_clause and intent iteself is insight_type, from_table_name
5. now using that where_clause which is with column name and its values will be placeholder. using the user input and RAG to map user input with exact column name i need to fill in the slots. our RAG will have metadata of column names. 
eg. i want client whose account balance greater that 1 million and client from xyz then where clause should be 
acct_bal >= 1000000 and city = 'xyz'. so this node will fil the slot and build sql from yaml conf for any other select column defn using the insight_name.yaml and this will have state with data fetch from table.
6. i have 1 app with main defn i need to call that to create narative. input for this will be target_type, insight_type, fetch_data etc
7. end
handle this agent for any error 
use the langgraph recent apis. i am using azure openai for LLM and chromadb for vector storage for embedding also use azure openai embedding model small. use openai/v1 endpoints.
8. expose this agent as a REST API using FastAPI and it should return stream response of the narrative generated by the app.
9. For agent memory use postgres as persistent storage to store user sessions, intents, and any other relevant data.
10. create a frontend using React and TypeScript to interact with the agent. 
The frontend should allow users to input their requirements in natural language, display the generated narrative, and show any relevant data fetched from the database.
11. For frontend auth use JWT-based authentication to secure the API endpoints. Implement login and registration functionality, and ensure that only authenticated users can access the agent's features.
12. Implement proper error handling and logging throughout the application to ensure that any issues can be diagnosed and resolved quickly.

I am using uv for env management.
